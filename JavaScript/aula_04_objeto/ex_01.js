let livro = {
    titulo:"pequeno principe",
    autor:"tom holand",
    ano:2018,
    pagina:299,
}

console.log (`o livro ${livro.titulo} do autor ${livro.autor} do ano de ${livro.ano} de ${livro.pagina} páginas`)